{
  "name": "listener",
  "version": "0.0.0",
  "description": "HTTP server that listens for ssid/password and saves them to wireless config",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.13.4"
  }
}
